var uiMapPanel = (
    // Public member
    return {
      "about" : function() {
        return "{ name: 'uiMapPanel', version : '0.1', desc : 'allow to add on panel on the left with link', depends : {" +
              + "} }";
      }
    };
  }
)();
